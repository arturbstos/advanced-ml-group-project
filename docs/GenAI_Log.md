# GenAI Transparency Log

## Project Information
- **Project Name:** German Freelancer Contract Analyzer
- **Repository:** https://github.com/BenediktKas/advanced-ml-group-project
- **Period:** 2025/2026 T4
- **Team Members:** 70170, 70365, 73630, 52011

---

## Summary Table / Tracker

| # | Date | Author | Tool | Phase | Output Used |
|---|------|--------|------|-------|-------------|
| 001 | 2026-04-20 | Team | Claude / ChatGPT | Planning | Yes |
| 002 | 2026-04-21 | Team | Claude / ChatGPT | Coding | Partially |
| 003 | 2026-04-21 | Team | Claude / ChatGPT | Coding | Partially |
| 004 | 2026-04-22 | Team | Claude / ChatGPT | Coding | Partially |
| 005 | 2026-04-22 | Team | Claude / ChatGPT | Coding | Partially |
| 006 | 2026-04-22 | Team | Claude / ChatGPT | Coding | Partially |
| 007 | 2026-04-23 | Team | Claude / ChatGPT | Coding | Partially |
| 008 | 2026-04-23 | Team | Claude / ChatGPT | Coding | Partially |
| 009 | 2026-04-23 | Team | Claude / ChatGPT | Writing | Partially |
| 010 | 2026-04-24 | 70170 | Claude (Cowork) | Review | Yes |
| 011 | 2026-04-25 | 70170 | Claude (Cowork) | Coding / Data prep | Yes |

---

## Detailed Entries

### Entry #001 — Architecture & Project Scaffold
- **Date:** 2026-04-20
- **Author:** Team
- **Tool:** Claude / ChatGPT
- **Phase:** Planning / Ideation
- **Prompt used:**
  > Design a 3-layer RAG architecture for a German freelance contract analyzer. Layer 1 should be a relational database of rate benchmarks and statute references. Layer 2 should be a vector store of curated risky-clause patterns (playbook). Layer 3 should be an LLM synthesis step that produces structured Findings with risk levels, legal reasoning, and suggested redlines.
- **Output used:** Yes — the three-layer architecture (relational facts → vector playbook → LLM synthesis) was adopted directly as the project's technical foundation. Folder structure (`app/`, `db/`, `scripts/`, `tests/`) and the `.env.example` scaffold were generated with AI assistance.
- **Modifications made:** Team reviewed the proposed architecture against the course requirements and Nova SBE capstone brief. Module names and file structure were adjusted to match the team's conventions.
- **Reason for using AI:** To rapidly prototype an architecture that meets both technical depth requirements and practical deployment constraints (Streamlit Cloud, hosted Postgres with pgvector).

---

### Entry #002 — Database Schema (`db/init.sql`)
- **Date:** 2026-04-21
- **Author:** Team
- **Tool:** Claude / ChatGPT
- **Phase:** Coding
- **Prompt used:**
  > Write a PostgreSQL schema for a German freelancer contract analyzer. Tables needed: `rate_benchmarks` (skill_category, region, experience, p25/median/p75 in EUR/h, source, source_year), `statute_references` (clause_type, paragraph, text_excerpt, official_url), and `playbook` (clause patterns with pgvector embeddings using text-embedding-3-small at 1536 dimensions). Add pgvector extension setup and appropriate indexes.
- **Output used:** Partially — the schema structure and pgvector setup were adopted. Team added the `[cite:]` architecture references, tightened CHECK constraints on the `experience` column, and reviewed column types against actual data sources (Freelancer-Kompass 2025, VGSD Survey 2024, BGB statutes).
- **Modifications made:** Added `UNIQUE (clause_type, paragraph)` constraint to `statute_references`; adjusted numeric precision for rate columns to `NUMERIC(6,2)`.
- **Reason for using AI:** To generate a correct pgvector schema quickly, avoiding boilerplate errors in SQL syntax for the vector extension.

---

### Entry #003 — Streamlit UI (`UX/UX.py`)
- **Date:** 2026-04-21
- **Author:** Team
- **Tool:** Claude / ChatGPT
- **Phase:** Coding
- **Prompt used:**
  > Build a clean, minimal Streamlit UI for a contract analyzer. It should have: a PDF upload widget, a toggle to use a sample contract instead, a summary row showing counts of high/medium/low findings, individual finding cards with risk badge (HIGH/MEDIUM/ACCEPTABLE), the original clause quoted verbatim, a 2–3 sentence analysis body, a suggested redline in a highlighted block, statute and source citations, and a collapsible Negotiation Brief section with a download button. Use Inter font, centered layout, max-width 780px, neutral color scheme.
- **Output used:** Partially — the component layout and CSS styling were generated by AI. Team manually adjusted color variables, removed AI-typical over-decoration (commit message: "Less AI Style"), and rewired the API call logic to match the FastAPI backend contract.
- **Modifications made:** Significant CSS cleanup; replaced several decorative elements; adjusted risk badge color logic; added the "Documents are not stored after the session ends" privacy notice.
- **Reason for using AI:** To bootstrap a professional UI rapidly without spending time on Streamlit layout boilerplate.

---

### Entry #004 — Contract Ingestion Pipeline (`app/services/ingestion.py`)
- **Date:** 2026-04-21 / 2026-04-22
- **Author:** Team
- **Tool:** Claude / ChatGPT
- **Phase:** Coding
- **Prompt used:**
  > Write an async Python function that (1) extracts text from a PDF using pdfplumber, (2) sends the text to GPT-4o-mini with a strict system prompt to extract structured contract fields verbatim, and (3) returns a Pydantic model with: skill_category, region, experience_level, hourly_rate_eur, payment_terms_days, and a list of verbatim clause strings. Emphasise that clauses must be extracted VERBATIM (not paraphrased) for downstream vector search to work correctly.
- **Output used:** Partially — the core extraction flow was adopted. Team hardened the prompt after testing showed paraphrased clauses degrading vector similarity scores (commit: `harden contract ingestion prompt and handle scanned PDFs`). The scanned PDF error path was also added manually after testing.
- **Modifications made:** Tightened the system prompt to repeat the verbatim instruction twice; added the `ValueError` for scanned/image-only PDFs with no extractable text; switched to `client.beta.chat.completions.parse` for structured output parsing.
- **Reason for using AI:** To generate the async OpenAI Structured Output pattern correctly and avoid common pydantic/async pitfalls.

---

### Entry #005 — Knowledge Layer: Playbook & Statute Seeds (`db/` data)
- **Date:** 2026-04-22
- **Author:** Team
- **Tool:** Claude / ChatGPT
- **Phase:** Coding / Research
- **Prompt used:**
  > Generate a gold-standard playbook of risky clause patterns for German freelancer (freie Mitarbeit) contracts. For each entry provide: clause_type (snake_case), risk_level (high/medium/low), pattern_description, example_risky_wording in German, legal_reasoning citing BGB/SGB IV/UrhG where applicable, and a recommended_redline in German. Cover at minimum: Scheinselbstständigkeit indicators, below-market hourly rates, late-payment interest below statutory default (§288 BGB), excessive IP assignment, non-compete clauses (Wettbewerbsverbot), and long payment terms.
- **Output used:** Partially — AI generated the initial set of playbook entries and statute references. Team reviewed each entry against primary German legal sources, corrected statute citations, and added the Freelancer-Kompass 2025 and VGSD Survey 2024 as rate benchmark sources.
- **Modifications made:** Corrected several BGB paragraph numbers; replaced generic legal reasoning with specific citations; added source_url fields pointing to official BGBL and dejure.org references.
- **Reason for using AI:** To generate a comprehensive initial playbook quickly; German contract law knowledge was then verified by team members with legal/business background.

---

### Entry #006 — Clause Analyzer & RAG Orchestration (`app/services/clause_analyzer.py`)
- **Date:** 2026-04-22
- **Author:** Team
- **Tool:** Claude / ChatGPT
- **Phase:** Coding
- **Prompt used:**
  > Write the orchestration layer for a 3-layer RAG contract analyzer. For each clause: (1) run vector search over a playbook table using pgvector, (2) fetch matching statute references from a relational table, (3) attach rate benchmark context if the clause concerns compensation, (4) send all context to GPT-4o-mini to synthesise a single Finding with risk level (high/medium/low), plain-language body in English, suggested German redline, and statute citation. Also add a synthetic rate-below-benchmark finding when the offered rate is below the p25 percentile. Return a list of Finding objects using Pydantic.
- **Output used:** Partially — the overall orchestration pattern and the `_SYSTEM_PROMPT` were AI-generated. Team tuned the risk classification rubric, added the synthetic rate-finding logic, and adjusted the LLM prompt after observing over-classification of medium-risk findings in testing.
- **Modifications made:** Rewrote the risk classification rubric (high/medium/low definitions); added the `_RATE_KEYWORDS` tuple for rate-clause detection; fixed the async session handling pattern to avoid connection leaks.
- **Reason for using AI:** The multi-step async orchestration across three data sources required careful sequencing; AI helped avoid common async/await pitfalls in SQLAlchemy 2.0.

---

### Entry #007 — Vector Seeding Script (`scripts/seed_vectors.py`)
- **Date:** 2026-04-22
- **Author:** Team
- **Tool:** Claude / ChatGPT
- **Phase:** Coding
- **Prompt used:**
  > Write a standalone async Python script that reads all rows from a `playbook` table where `embedding IS NULL`, composes a semantic text blob from each row's fields (clause_type, pattern_description, example_risky_wording, recommended_redline, legal_reasoning), embeds each blob using OpenAI `text-embedding-3-small` (1536-dim), and writes the resulting vector back to the `embedding` column using pgvector. Include a `--force` flag to re-embed all rows.
- **Output used:** Partially — the embedding loop and pgvector literal formatting were adopted. Team added the `_compose_text` cross-lingual note (text-embedding-3-small handles German/English mixed input), the per-row progress output, and the `--force` flag handling.
- **Modifications made:** Added explicit dimension validation (checking `len(vec) == 1536`) to catch model API changes; adjusted the text blob composition order to weight clause_type and pattern_description most heavily.
- **Reason for using AI:** Generating correct pgvector literal string formatting from Python and async SQLAlchemy update patterns is error-prone boilerplate.

---

### Entry #008 — Bug Fixes: Env Loading, Upload Safety, Markdown Parser
- **Date:** 2026-04-23
- **Author:** Team
- **Tool:** Claude / ChatGPT
- **Phase:** Coding / Review
- **Prompt used:**
  > The Streamlit app is crashing on startup because `load_dotenv()` is called after `os.getenv()`. Fix the env-load ordering. Also: the file uploader does not safely clean up temporary files if an exception is raised mid-analysis — add a try/finally block. Additionally the markdown rendering is wrapping HTML output in code blocks — fix the Streamlit markdown parser to render HTML correctly.
- **Output used:** Partially — all three fixes were implemented with AI assistance. The `fix(ui): prevent markdown parser from rendering html as code blocks` and `fix: env-load ordering, upload safety, multipart dep, statute seeds` commits reflect these changes.
- **Modifications made:** Team verified each fix against the actual error logs before committing; the multipart dependency (`python-multipart`) addition to `requirements.txt` was identified independently during testing.
- **Reason for using AI:** Debugging subtle async/env ordering issues and Streamlit markdown rendering quirks is faster with AI assistance than manual documentation search.

---

### Entry #009 — README & Setup Documentation
- **Date:** 2026-04-23
- **Author:** Team
- **Tool:** Claude / ChatGPT
- **Phase:** Writing
- **Prompt used:**
  > Write a complete README.md for a GitHub repository hosting a German Freelancer Contract Analyzer. Include: project overview, 3-layer architecture description, prerequisites, local setup instructions (Docker for Postgres+pgvector, .env config, pip install, seed_vectors, uvicorn + streamlit), running tests, deployment notes for Streamlit Cloud, and a license section.
- **Output used:** Partially — structure and setup instructions were generated by AI and then reviewed and corrected by the team to match the actual repo state (correct env variable names, actual command syntax).
- **Modifications made:** Team corrected several command paths, added the Streamlit Cloud deployment-specific environment variable instructions, and removed sections that did not apply to the current repo state.
- **Reason for using AI:** Documentation generation from an existing codebase is a well-suited AI task; human review ensured accuracy.

---

### Entry #010 — App Testing via Claude Cowork (Browser Automation)
- **Date:** 2026-04-24
- **Author:** 70170
- **Tool:** Claude (Anthropic Cowork, desktop app)
- **Phase:** Review / Testing
- **Prompt used:**
  > Test the deployed Streamlit app end-to-end. Navigate to the live URL, verify the page loads, check all findings render correctly with proper risk badges, test the "View full brief" expander, and verify the Download brief button is present.
- **Output used:** Yes — Claude used browser automation (Claude in Chrome extension) to navigate to `https://advanced-ml-group-project-ft7z36syhjzjnuwkfgsthk.streamlit.app/`, scroll through all 4 findings (1 HIGH, 2 MEDIUM, 1 ACCEPTABLE), open the Negotiation Brief expander, and confirm the Download brief button renders. All UI elements passed visual inspection. PDF upload via browser automation was blocked by Chrome extension security restrictions and was not completed.
- **Modifications made:** N/A — this was a read-only testing session. Findings documented for follow-up: (1) no automated tests exist yet, (2) PDF upload needs manual testing.
- **Reason for using AI:** Automated browser testing of a deployed Streamlit app without writing a Selenium/Playwright test suite.

---

### Entry #011 — Rate Benchmark Seeding from Freelancer-Kompass 2025
- **Date:** 2026-04-25
- **Author:** 70170
- **Tool:** Claude (Anthropic Cowork, desktop app)
- **Phase:** Coding / Data prep
- **Prompt used:**
  > Read the Freelancer-Kompass 2025 PDF (page 34, "Stundensatz nach Fachgebiet"). Extract the eight skill-category medians. Produce `db/seed_rates.sql` that seeds the `rate_benchmarks` table with 24 rows (8 categories × 3 experience tiers junior/mid/senior). Use English skill_category labels. Derive p25/p75 transparently and document the methodology in README.md. Also add a `_normalize_skill_category()` function to `db/rate_lookup.py` that maps free-text LLM output (German or English, narrow or broad) onto the canonical English buckets, with order-sensitive substring matching so e.g. "DevOps engineer" → IT Infrastructure, "Mechanical engineer" → Engineering, "SAP ABAP developer" → SAP Consulting.
- **Output used:** Yes — three deliverables shipped:
  1. **`db/seed_rates.sql`** — 24 INSERT rows wrapped in a transaction, idempotent via `DELETE ... WHERE source = 'Freelancer-Kompass 2025'`. The header documents the source, the raw medians, and the two-step derivation: experience multiplier of ±30 % on the category median (`junior × 0.70`, `senior × 1.30`) and within-tier dispersion of ±15 % (`p25 = tier × 0.85`, `p75 = tier × 1.15`).
  2. **`db/rate_lookup.py`** — added `CANONICAL_SKILL_CATEGORIES`, the keyword table `_SKILL_KEYWORDS`, and `_normalize_skill_category()`. Wired into `lookup()` so all incoming free-text categories pass through the normalizer before the SQL bind. 22 unit-style cases verified against the function (covering DE+EN, edge cases like empty/None, and the disambiguation cases SAP-vs-Software-Development and Mechanical-vs-Software).
  3. **`README.md`** — new step "3. Seed Rate Benchmarks" between env config and playbook seeding (renumbering existing step 3→4); new "## Methodology" section at end covering rate benchmark derivation (with DE↔EN translation table), rationale for the multipliers (broadly consistent with Eurostat SES patterns for ISCO-08 groups 21/25; conservative vs GULP p25/p75 spreads), region-NULL choice, and a "Deferred Enrichments" subsection enumerating what we deliberately did **not** seed (industry, education premium, service-type, year-over-year, gender split).
- **Modifications made:** Reordered Engineering before Software Development in `_SKILL_KEYWORDS` after a unit-test failure showed "Mechanical engineer" was being caught by Software Development's generic "engineer" keyword. Added German "sicherheit" to IT Infrastructure keywords after "IT-Sicherheit Berater" was misclassified as Consulting & Management.
- **Reason for using AI:** PDF extraction (vision over a 46-page report), generation of structured SQL with documented methodology, and authoring the keyword-disambiguation table are tasks AI tools handle efficiently. The team retained authority over all judgment calls — the choice of ±30 % / ±15 % multipliers, the decision to seed nationwide only, and the deferred-enrichments scope.

---

## Notes & Reflections
- AI tools (Claude, ChatGPT) were used throughout the project primarily for code scaffolding, boilerplate generation, and debugging assistance.
- All AI-generated code was reviewed by at least one team member before committing. Prompts are reproduced above as accurately as possible from memory and git history.
- The core intellectual contributions — 3-layer RAG architecture design, choice of German legal sources (BGB, Freelancer-Kompass 2025, VGSD Survey 2024), playbook content, and risk classification rubric — were developed by the team and refined through AI-assisted iteration.
- LLM models used in production: `gpt-4o-mini` (clause analysis and contract extraction), `text-embedding-3-small` (playbook vector embeddings).
- AI tools were used to support, not replace, our own thinking. All code generated by AI was reviewed and tested by team members. Prompts and outputs were critically evaluated before use.

---

*Last updated: 2026-04-25 by 70170*
